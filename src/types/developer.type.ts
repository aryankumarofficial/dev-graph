export interface IDeveloper {
    id: string;
    name: string;
    username: string;
    bio: string;
}

export interface DeveloperSkill {
    id: string;
    name: string;
    category: string;
}

export interface DeveloperTechnology {
    id: string;
    name: string;
    category: string;
    description: string;
}